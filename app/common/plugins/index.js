// Plugins are third party modules that are unavailable as bower installations.
// All of the code in these plugins has been created by a third party.

'use strict';

module.exports = angular.module('common.plugins', [
		require('./pagination').name
	]);